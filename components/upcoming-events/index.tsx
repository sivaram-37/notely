import CardWrapper from "../common/cardWrapper";

const UpcomingEvents = () => {
  return <CardWrapper className="flex items-center justify-center">UpcomingEvents</CardWrapper>;
};

export default UpcomingEvents;
